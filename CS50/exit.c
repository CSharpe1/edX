#include <stdio.h>

int main(int argc, string argv[])
{
    if (agrc != 2)
    {
        printf("Missing comand-line argument\n")
        return 1;
    }
    printf("Hello, %s\n", argv[1]);
    return 0;
}